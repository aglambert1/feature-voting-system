# Render Blueprint — defines all services for one-click deploy
# https://render.com/docs/blueprint-spec

services:
  # Backend API
  - type: web
    name: feature-iq-api
    plan: starter
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    healthCheckPath: /health
    preDeployCommand: "alembic stamp head"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: feature-iq-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: feature-iq-redis
          type: redis
          property: connectionString
      - key: ANTHROPIC_API_KEY
        sync: false  # Set manually in dashboard
      - key: BRAVE_API_KEY
        sync: false
      - key: VOYAGE_API_KEY
        sync: false
      - key: DEBUG
        value: "false"
      - key: ALLOWED_ORIGINS
        value: '["https://feature-iq.onrender.com"]'
      - key: ADMIN_EMAIL
        sync: false
      - key: ADMIN_USERNAME
        sync: false
      - key: ADMIN_PASSWORD
        sync: false

  # Celery worker for background tasks (paid plan required)
  - type: worker
    name: feature-iq-worker
    plan: starter
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    dockerCommand: celery -A app.queue worker --loglevel=info --pool=solo
    envVars:
      - key: SECRET_KEY
        fromService:
          name: feature-iq-api
          type: web
          envVarKey: SECRET_KEY
      - key: DATABASE_URL
        fromDatabase:
          name: feature-iq-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: feature-iq-redis
          type: redis
          property: connectionString
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: BRAVE_API_KEY
        sync: false
      - key: VOYAGE_API_KEY
        sync: false
      - key: DEBUG
        value: "false"

  # MCP HTTP Server (remote MCP client access)
  - type: web
    name: feature-iq-mcp
    plan: starter
    runtime: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    dockerCommand: python -m mcp_server.http_server
    envVars:
      - key: SECRET_KEY
        fromService:
          name: feature-iq-api
          type: web
          envVarKey: SECRET_KEY
      - key: DATABASE_URL
        fromDatabase:
          name: feature-iq-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: feature-iq-redis
          type: redis
          property: connectionString
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: VOYAGE_API_KEY
        sync: false
      - key: MCP_BASE_URL
        value: "https://feature-iq-mcp.onrender.com"
      - key: FRONTEND_URL
        value: "https://feature-iq.onrender.com"
      - key: DEBUG
        value: "false"

  # Frontend static site
  - type: web
    name: feature-iq
    runtime: static
    buildCommand: cd frontend && npm ci && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: "https://feature-iq-api.onrender.com"
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # Redis for Celery message broker
  - type: redis
    name: feature-iq-redis
    plan: free
    maxmemoryPolicy: allkeys-lru
    ipAllowList: []  # Only internal services can connect

databases:
  - name: feature-iq-db
    plan: Basic-256mb
    databaseName: feature_iq
    postgresMajorVersion: "16"
