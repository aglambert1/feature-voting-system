# Core FastAPI framework
fastapi==0.115.0

# ASGI server for running FastAPI
uvicorn[standard]==0.32.0

# Database ORM (Object-Relational Mapping) - helps us work with database using Python classes
sqlalchemy==2.0.36

# Database driver for PostgreSQL
psycopg2-binary==2.9.10

# Data validation - ensures data has correct types and format
pydantic[email]==2.10.0

# Settings management - loads environment variables
pydantic-settings==2.6.0

# JWT token creation and validation for authentication
python-jose[cryptography]==3.3.0

# Password hashing - securely stores passwords
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Form data handling (needed for OAuth2 password flow)
python-multipart==0.0.6

# Testing framework
pytest==7.4.4
pytest-asyncio==0.23.3

# HTTP client for testing
httpx==0.26.0

# Requests library for test_api.py script
requests==2.32.5

# Anthropic Claude API
anthropic==0.42.0
